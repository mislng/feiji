# Task Queue

Task files can be organized by lifecycle state:

- `backlog/` — ideas and unprioritized work
- `ready/` — implementation-ready tasks
- `doing/` — currently being implemented
- `review/` — awaiting review or verification
- `done/` — completed task records

Each task should follow the contract described in `TASKS.md`.
